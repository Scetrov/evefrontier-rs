{
  "name": "evefrontier-service-scout-range",
  "projectType": "application",
  "sourceRoot": "crates/evefrontier-service-scout-range/src",
  "tags": ["scope:service", "type:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo build -p evefrontier-service-scout-range",
        "cwd": "{workspaceRoot}"
      },
      "outputs": ["{workspaceRoot}/target/debug/evefrontier-service-scout-range"]
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "cargo test -p evefrontier-service-scout-range",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo fmt -p evefrontier-service-scout-range -- --check",
        "cwd": "{workspaceRoot}"
      }
    },
    "clippy": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "cargo clippy -p evefrontier-service-scout-range --all-targets --all-features -- -D warnings",
        "cwd": "{workspaceRoot}"
      }
    },
    "complexity": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "cargo clippy -p evefrontier-service-scout-range --all-targets -- -W clippy::cognitive_complexity -W clippy::too_many_lines",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
